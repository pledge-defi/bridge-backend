pragma solidity ^0.6.12;

contract ReleaseRules {
    // uint256 mPlgrPrice public;
    // totalRelease = base * delta;
    uint256 public delta;
    uint256 public base;
    
    constructor() public {

    }

    // （用户A lock 数量/所有用户总的lock数量 ）* 当前总的释放量

}