contract PledgerBridgeBSC {
    address keeper_owner;

    // User call this function on ETH to widthdraw MPLGR.
    function widthdraw_mplgr(bytes calldata data) external {

    }

    // Chainbridge call this function on ETH
    function deposit_mplgr_rule(bytes calldata data) external {

    }
}
